<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Bakti-Maba</title>
    <style>
        .material-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }
        .file-icon {
            background: linear-gradient(135deg, #3b82f6, #60a5fa);
        }
        .popular-badge {
            background: linear-gradient(135deg, #f59e0b, #fbbf24);
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="flex">
        
        <div class="flex-1 p-6">
            <div class="bg-white rounded-xl shadow p-6 mb-6">
                <div class="flex justify-between items-center">
                    <h1 class="text-2xl font-bold text-gray-800">Materi</h1>
                    <div class="w-1/3">
                        <input type="text" id="searchInput" onkeyup="searchMaterials()" placeholder="Cari materi..." class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm">
                    </div>
                </div>
            </div>
            
            <div class="grid grid-cols-1 gap-6" id="materialsList">
                <% if (materials.length > 0) { %>
                    <% materials.forEach(material => { %>
                        <div class="material-card bg-white rounded-xl shadow p-5">
                            <div class="flex justify-between items-start">
                                <div>
                                    <h2 class="text-lg font-bold text-gray-800"><%= material.judul_materi %></h2>
                                    <p class="text-gray-600 mt-2 text-sm"><%= material.deskripsi_materi %></p>
                                </div>
                                <a href="/material/download/<%= material.id_materi %>" class="ml-4 flex-shrink-0 bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg text-sm">
                                    Unduh
                                </a>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="text-center py-10 bg-white rounded-xl shadow">
                        <svg xmlns="http://www.w3.org/2000/svg" class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                        <h3 class="mt-2 text-sm font-medium text-gray-900">Belum ada materi</h3>
                        <p class="mt-1 text-sm text-gray-500">Materi akan segera tersedia setelah diunggah oleh panitia.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script>
    function searchMaterials() {
        const input = document.getElementById('searchInput');
        const filter = input.value.toUpperCase();
        const list = document.getElementById('materialsList');
        const cards = list.getElementsByClassName('material-card');
        for (let i = 0; i < cards.length; i++) {
            let title = cards[i].getElementsByTagName('h2')[0];
            if (title) {
                let txtValue = title.textContent || title.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    cards[i].style.display = "";
                } else {
                    cards[i].style.display = "none";
                }
            }
        }
    }
    </script>
</body>
</html>