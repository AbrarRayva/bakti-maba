<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Penugasan</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .tabs {
            display: flex;
            margin-bottom: 20px;
            border-bottom: 1px solid #ddd;
        }
        .tab-button {
            padding: 10px 20px;
            cursor: pointer;
            border: none;
            background: none;
            font-size: 1.1em;
            font-weight: bold;
            color: #555;
            transition: color 0.3s, border-bottom 0.3s;
        }
        .tab-button.active {
            color: #6a0dad; /* Purple */
            border-bottom: 2px solid #6a0dad;
        }
        .assignment-list-container {
            margin-top: 20px;
        }
        .assignment-item {
            background-color: white;
            padding: 15px 20px;
            margin-bottom: 15px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .assignment-item .assignment-info {
            flex-grow: 1;
        }
        .assignment-item .assignment-info h3 {
            margin: 0 0 5px 0;
            color: #333;
            font-size: 1.2em;
        }
        .assignment-item .assignment-info p {
            margin: 0;
            color: #666;
            font-size: 0.9em;
        }
        .assignment-item .actions-dropdown {
            position: relative;
            display: inline-block;
        }
        .assignment-item .actions-button {
            background: none;
            border: none;
            font-size: 1.5em;
            cursor: pointer;
            color: #888;
        }
        .assignment-item .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 120px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
            right: 0;
            border-radius: 5px;
        }
        .assignment-item .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }
        .assignment-item .dropdown-content a:hover {
            background-color: #f1f1f1;
        }
        .assignment-item .actions-dropdown:hover .dropdown-content {
            display: block;
        }
    </style>
</head>
<body>
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-2">Daftar Penugasan</h1>
        <p class="text-gray-600 mb-8">Berikut adalah daftar semua tugas yang harus dikerjakan.</p>

        <div class="space-y-4">
            <% if (assignments.length > 0) { %>
                <% assignments.forEach(assignment => { %>
                    <a href="/assignment/<%= assignment.id_tugas %>" class="block bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300">
                        <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center">
                            <div class="flex-grow mb-4 sm:mb-0">
                                <h2 class="text-xl font-semibold text-gray-800 mb-1"><%= assignment.nama_tugas %></h2>
                                <p class="text-sm text-gray-500">
                                    <i data-lucide="calendar" class="inline-block w-4 h-4 mr-1"></i>
                                    Batas Waktu: <%= new Date(assignment.deadline).toLocaleString('id-ID', { dateStyle: 'full', timeStyle: 'short' }) %>
                                </p>
                            </div>
                            <div class="flex-shrink-0">
                                <% const submission = submissionMap[assignment.id_tugas]; %>
                                <% if (submission) { %>
                                    <span class="px-4 py-2 rounded-full text-sm font-semibold bg-green-100 text-green-800">
                                        <i data-lucide="check-circle-2" class="inline-block w-4 h-4 mr-1"></i>
                                        Terkumpul
                                    </span>
                                <% } else if (new Date(assignment.deadline) < new Date()) { %>
                                    <span class="px-4 py-2 rounded-full text-sm font-semibold bg-red-100 text-red-800">
                                        <i data-lucide="x-circle" class="inline-block w-4 h-4 mr-1"></i>
                                        Terlewat
                                    </span>
                <% } else { %>
                                    <span class="px-4 py-2 rounded-full text-sm font-semibold bg-blue-100 text-blue-800">
                                        <i data-lucide="alert-circle" class="inline-block w-4 h-4 mr-1"></i>
                                        Tersedia
                                    </span>
                                <% } %>
                            </div>
                        </div>
                    </a>
                    <% }) %>
                <% } else { %>
                <div class="text-center py-16 bg-white rounded-lg shadow-md">
                    <i data-lucide="folder-open" class="w-16 h-16 mx-auto text-gray-300 mb-4"></i>
                    <h3 class="text-xl font-semibold text-gray-700">Belum Ada Tugas</h3>
                    <p class="text-gray-500 mt-2">Saat ini belum ada tugas yang diberikan.</p>
                </div>
                <% } %>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const tabButtons = document.querySelectorAll('.tab-button');
            const tabContents = document.querySelectorAll('.tab-content');

            function showTab(tabId) {
                tabContents.forEach(content => {
                    if (content.id === tabId) {
                        content.style.display = 'block';
                    } else {
                        content.style.display = 'none';
                    }
                });
                tabButtons.forEach(button => {
                    if (button.dataset.tab === tabId) {
                        button.classList.add('active');
                    } else {
                        button.classList.remove('active');
                    }
                });
            }

            tabButtons.forEach(button => {
                button.addEventListener('click', () => {
                    showTab(button.dataset.tab);
                });
            });

            // Show default tab on load (e.g., 'on-going')
            showTab('on-going');
        });
    </script>
</body>
</html>